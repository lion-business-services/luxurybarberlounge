# Hero opening visual adjustment

The opening hero fallback/background now uses the clean video poster instead of the separate lounge-wall artwork that contained the oversized blurred crest. The ambient hero MP4 remains enabled and unchanged, including autoplay, looping, poster fallback, offscreen pause behavior, responsive conditions, and reduced-motion handling.
