"use client";
import { useEffect } from "react";
import Link from "next/link";
import { RefreshCw } from "lucide-react";
export default function ErrorPage({error,reset}:{error:Error&{digest?:string};reset:()=>void}){useEffect(()=>{console.error(error)},[error]);return <main className="grid min-h-[70svh] place-items-center px-6 py-20 text-center"><section className="max-w-xl"><p className="text-[10px] tracking-[.3em] uppercase text-[var(--color-brass)]">Something interrupted the service</p><h1 className="font-display mt-5 text-5xl">The page needs another pass.</h1><p className="mt-5 text-sm leading-7 text-[var(--color-bone-muted)]">No booking or payment should be assumed complete after an application error. Retry, return home, or contact the lounge directly.</p><div className="mt-8 flex flex-wrap justify-center gap-3"><button type="button" onClick={reset} className="inline-flex items-center gap-2 rounded-full bg-[var(--color-brass)] px-6 py-3 text-[10px] tracking-[.2em] uppercase text-[var(--color-ink)]"><RefreshCw className="h-4 w-4"/> Retry</button><Link href="/" className="rounded-full border border-[var(--color-ink-line)] px-6 py-3 text-[10px] tracking-[.2em] uppercase">Return home</Link></div>{error.digest?<p className="mt-7 text-[10px] text-[var(--color-bone-muted)]">Reference {error.digest}</p>:null}</section></main>}
