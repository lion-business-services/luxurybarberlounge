import type { Metadata } from "next";
import { PageHero } from "@/components/PageHero";
import { WalkInFlow } from "@/components/queue/WalkInFlow";
import { CtaBand } from "@/components/public/PublicUI";
export const metadata:Metadata={title:"Walk-Ins & Queue",description:"Join the Luxury Barber Lounge walk-in queue or review how check-in works.",alternates:{canonical:"/walk-ins"}};
export default function WalkIns(){return <><PageHero eyebrow={{en:"When the day opens up",es:"Cuando el día se abre"}} title={{en:"Walk-Ins & Queue",es:"Walk-Ins y Fila"}} lead={{en:"Choose a service, preferred chair, and private contact method. Appointments remain the most reliable way to reserve time.",es:"Elige servicio, silla preferida y contacto privado. Las citas son la forma más segura de reservar."}}/><main className="mx-auto max-w-6xl px-6 pb-28 sm:px-10"><div className="grid gap-8 lg:grid-cols-[1.2fr_.8fr]"><WalkInFlow/><aside className="space-y-5"><Info n="01" title="Check in" copy="Use the mobile page or in-lounge tablet. The kiosk resets between guests and never shows full names publicly."/><Info n="02" title="Receive an estimate" copy="The production queue considers service duration, barber preference, current workload, booked appointments, and manual reception decisions."/><Info n="03" title="Stay informed" copy="Once SMS is activated, transactional updates can confirm waiting, called, ready, reassigned, or cancelled status."/></aside></div></main><CtaBand eyebrow="Prefer certainty?" title="Reserve the chair before you arrive." /></>}
function Info({n,title,copy}:{n:string;title:string;copy:string}){return <article className="border border-[var(--color-ink-line)] p-6"><p className="text-[9px] tracking-[.25em] uppercase text-[var(--color-brass)]">{n}</p><h2 className="font-display mt-3 text-2xl">{title}</h2><p className="mt-3 text-sm leading-7 text-[var(--color-bone-muted)]">{copy}</p></article>}
