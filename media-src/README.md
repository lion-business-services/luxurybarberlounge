# Source media (not served)

Masters kept out of `public/` so they are never deployed to the CDN:

- `lounge-entry-source.mp4` — original threshold film; web encodes live at
  `public/media/home/video/lounge-entry.{mp4,webm}`.
- `logo-uploaded-2026.png` — original logo master; production derivatives live
  in `public/brand/`.
